#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - writes the character c to stdout
 *e
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int main(void)

{
	printf("_putchar\n");
	return (0);
}
